name: Daily Cron Job

on:
  schedule:
    - cron: "0 0 * * *"   # roz raat 12 baje run hoga

jobs:
  cron-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "This is a scheduled cron job running daily!"